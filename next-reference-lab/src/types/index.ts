export type Profile = { id: string; name: string; role: string; bio: string };
export type Task = { id: number; title: string; completed: boolean };

export type User = {
  id: string;
  name: string;
  job: string;
};
